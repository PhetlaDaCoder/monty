#include "monty.h"

/**
 * f_nop - a function that does nothin.
 * @head: stack head
 * @counter: line number.
 * Return: 0
 */
void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
